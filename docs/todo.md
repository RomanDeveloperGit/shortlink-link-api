1. Внедрить ADR Docs
2. Записать в ADR, что пока что генерация ссылки и сам переход по ссылке живут в одном сервисе, потому что нагрузка маленькая. Но всё же есть кэш. Дальнейшие варианты - отдельные сервисы для создания и для переходов. Возможно, CQRS
3. Изначально внедрить только PostgreSQL + Rest API в одном сервисе и зафиксировать в ADR. Далее новый ADR - типа нагрузка увеличилась, хотим улучшить UX и добавляем Редис-кэш для перфоманса (учитывать, что он может упасть и при поднятии на один и тот же ключ может сервер начать писать в кэш значения, поэтому надо залочить. Рассмотреть AOF/RDB), чтобы пока что не выносить в разные сервисы это добро. Если кэша не хватит, придется это делать
4. Добавить джобу для удаления протухших ссылок (которые протухли уже больше 7 дней назад). Джоба выполняется ночью, в самый "тихий" по нагрузке момент. Батчами и вакуум делать

4. Генерация коротких ссылок
5. Отправка этих ссылок указанным клиентам на почту
6. Время жизни ссылок
7. Нужно собирать самые часто-используемые ссылки в статистике (statistics-api(будет ли разделение на statistics-consumer?), через kafka слушает события и пишет себе. CQRS уже выходит? ClickHouse юзаем? Альтернативы под задачу)
8. Kafka, Redis, Postgres, API gateway (with reverse-proxy), логирование + мониторинг (logstash, elasticsearch, kibana, prometheus, grafana, opentelemetry, jaeger)
9. Lefthook tests (unit / e2e ) + CI/CD
10. Можно добавить фоновый воркер, который отправляет отчёты по самым часто-используемым ссылкам за каждый день
11. Можно добавить какой-нибудь admin-dashboard-api, который ходит по gRPC в link-api и в statistics-api(будет ли разделение на statistics-consumer?), агрегирует данные в рамках своих ручек и выдает для пользователя-админа условного CRM
